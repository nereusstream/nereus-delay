# NDIP-3 执行记录

> **当前执行阶段：先完成全部实际运行代码，最后集中测试（用户于 2026-09-08 调整）。**
> 保持当前模型、单 agent、直接 main、分阶段 scoped commit/push。先贯通原 29 切片的
> 代码、运行路径装配、配置及迁移/清退工具；完整回归、真实 Broker、性能、故障恢复
> 和依赖验证的实际迁移/清退，留待用户切换模型后集中执行。当前只保留编译、格式/
> 文档一致性和影响开发正确性的最小局部检查；不需要现在确认或切换模型。
> 原目标总范围不变，实现状态与验证状态分别记录；未验证实现不得标记 VERIFIED、
> 提案治理 Implemented 或方案完成。

[原 29 切片集中验证交接清单](12-集中验证交接清单.md)

## 2026-09-07：导入与基线核对

- `main@b521b614fbe22d30381593637e60ab615e5906cc`，初始工作树干净。
- 已通读完整 1648 行设计；输入 SHA-256 与交接一致。
- 本目录和父目录未发现适用 AGENTS.md；其他仓库规则不跨目录套用。
- 已读取现行 NDIP 索引、Accepted NDIP-2 与 IMPLEMENTATION-STATUS。
- 已建立覆盖 29 个切片的 active goal；不设置 token_budget、不创建子 agent。
- 当前正在执行 A0；尚无本方案 Java/Broker PASS 或迁移事实。

初始文档门：`checkDocumentation` PASS，`checkProjectVersionMarkers` 发现两处 Profile 版本
例子使用被禁止的项目编号缩写，已改为显式 `Profile version 1/2`，未放宽 gate。
新增 runtime 后，文档门改为使用现有 `--historical-only` 验证 NDIP-1 的不可变关闭证据；
`verifyNdip1Implemented` 原有当前源码等价验证保留为严格独立任务，当前新源码不声称等价。

## A0 独立验收

`DelayShardHeadSelectionTest` 2/2、`NativeManagedScheduleProjectionTest` 1/1、
`KeyCodecTest` 10/10 通过，零 skip。全扫 oracle 使用固定随机 seed=20260907。
N=16/64、L=1、payload=7 bytes：候选与 Message 读取分别 120/2016；quota prepared
bytes 分别 10016/40156。范围为组件逻辑读取，不是端到端 TPS 或磁盘 I/O。
精确源码文件 digest 与测试名保存在 `evidence/a0-baseline.json`。

`checkDocumentation checkProjectVersionMarkers checkstyleMain checkstyleTest spotlessCheck`
最终 PASS（22 tasks，2026-09-07）。NDIP verifier 回归 10/10，package verifier 6/6。
旧源码等价测试改为历史关闭验证，并新增受控 exact source/path drift 正反例；
严格 `verifyNdip1Implemented` 没有降低要求。初次编译的测试 API 类型错误已修正，
初次旧源码等价 gate 失败据上述治理分轨处理，没有重签历史 receipt。

A0 完成，A1 进行中；A2–F3 保持未完成。真实 Broker、迁移尚未执行。

## A1 独立验收

完整 `./gradlew spotlessApply check --console=plain` PASS，Java 1688 项，
失败 0、错误 0、skip 41。skip 是缺少外部服务/认证环境
的既有 opt-in 测试，明细在 `evidence/a1-results.json`；不作为真实 Broker PASS。
在完整门前，6 个相关测试类合计 220 项零 skip 通过。

N=16/64 的连续 Schedule 候选/Message 读量变为 15/63；quota 字节未减少，仍待 C4。
对直接构造的 RocksDB 有序索引组件，N=1k/10k/100k 的首项/删头/删非头/排除64项读量
均为 1/2/1/65；不将此组件测试当作完整 Schedule 或端到端性能。

A0 提交 `049dbd04e65a86a6f6169741d90b0ae8f611b611` 已推送并核验远端。
默认 SSH 22 连接关闭；HTTPS 的现有账号只有读取权限、push 返回403；最终使用 SSH 443
和已有 github.com host key 的严格验证成功。只对调用设置 URL/SSH 参数，没有修改全局凭据。
A1 完成，A2 进行中；完整目标保持 active。

## A2 存储基础批次（切片未完成）

三态 head、显式 Store visit 停止原因、共享记录/字节/时间预算、依赖 get 计费及 ReadView
提交绑定已加入。6 个新增负例覆盖 seek 前耗尽、删除 head 后耗尽、Message 点查前耗尽、
真实返回字节、tail/wrap 共用额度，以及跨写入/Owner 元数据/Store 重开的旧 view 拒绝。

`./gradlew check --console=plain` PASS，Java 1694 项，失败 0、
错误 0、skip 41。skip 仍不构成外部服务证据。
精确运行源码文件 digest 和日志在 `evidence/a2-storage-foundation-*`。
A1 提交 `5cdd252e6bb0e1d9696b9cfb86edc5dd501fb83b` 已推送且远端 SHA 核对一致。

下一批继续 A2：把整个 readyProjections / 控制及 Claim plan 放在共同预算内；把完成的
ReadView 与 Shard mutation/source/Owner 边界一起校验；接通 SourceApplyCoordinator 和
OwnedDelayShard 的本地 retry 分类，避免把预算让出 fence 成普通未知写；为 READY
旋转 discovery 的 tail/wrap 及点查共享预算。需要核对 `actionAtFor` 的 legacy
Claim/Admission/binding 依赖（可能含 inflight scan），不能只限制主候选读取。

最大合法单消息 byte envelope 仍需源码证明：ScheduleBinding 限制 tuple=1MiB、body=16MiB，
但 MessageRecord/runtime/legacy 依赖不能未经核对直接声称受某个较小常数限制。配置校验、
反复 elapsed 耗尽诊断和所有未提交负例完成后才可将 A2 标 VERIFIED。A3–F3 均仍待完成。


## A2 READY discovery 批次（切片未完成）

已完成旋转 READY 的共享 records/bytes/elapsed、Lane/Message/timeline/native binding
依赖计费、完整投影发布和视图提交绑定。Store 的 visitor 依赖耗尽仍执行 iterator.status；
预算让出不掩盖 I/O error。native policy callback 在 Store monitor 外并检查同一 elapsed
截止时间；回调后在 Store monitor 内复核 view，再发布队列和 scheduler batch。

新增 7 项边界测试覆盖 Message 点查耗尽、tail/wrap 不重置、elapsed 点查前让出、未来
native wrapped 投影不推进到期 cursor、跨 view 发布拒绝、visitor 依赖耗尽关闭 iterator，
以及完整输入的后续处理共享截止时间而不重置物理记录额度。

完整 `./gradlew spotlessApply check --console=plain` PASS，Java 1701 项、失败 0、
错误 0、skip 41；其中 LaneScheduler/ShardReadPlan/ClaimHandoff/DueScheduler 四类
合计 60 项零 skip 通过。此前首次完整检查暴露 Claim handoff 的 4 KiB fixture 只按旧
READY 字节计费；现改为按该 fixture 实际 READY + 两次 Lane + Message + timeline
字节建立预算并逐字节核对，不把测试预算当作全量合法消息的资源认证。

精确源码/测试文件 digest、skip 清单和运行日志在 `evidence/a2-ready-discovery-*`。
A2 存储基础提交 `a61df6b09ba4fc873acf6cc6c079fdf1e4713d53` 已推送，批次前 fetch
确认本地与 origin/main 无 ahead/behind；本批继续单 agent、直接 main、显式路径发布。

A2 下一步仍为 head mutation/control/Claim 的共同有限预算、完整 HeadPlan 的
Shard mutation/source/Owner 校验与 Source Apply/恢复的本地重试分类。额外源码核对确认
Kafka/Pulsar source identity、PayloadReference 的若干可变长字段没有可直接沿用的较小
常量上限；legacy actionAt 依赖可扫描有界但较大的 inflight 集合。因此最大合法 plan 的
完整 envelope 必须继续与已激活 Route/Profile/Store 资源约束核对，不能凭 READY 的
5/7/8 条读取路径推断整个 A2 或完整方案已经完成。A3–F3、真实 Broker 和迁移仍待执行。


## A2 HeadPlan 与 source/Claim/recovery 批次（切片未完成）

整个 readyProjections 的 DUE/ORDERED/NATIVE、Lane/Message 及 legacy Store 依赖共用
一个 records/bytes/elapsed 预算。22 个投影写入口在构造 WriteBatch 前，于 Store monitor
内校验全部完整 HeadPlan 的 Store/Owner、mutationSequence 和 source 视图；batch 内
保留第二次校验。terminal retirement 的无工作判断也携带同样的读取证明。

只有整个 mutation 的 Store/source/sequence 均未变化才分类为本地 READ_INCOMPLETE；
外层操作已经写入、未知 native 写失败或其他 work class 失败均不能借此跳过 fencing。
SourceApplyCoordinator 保留原 source，不 ACK、不推进；恢复保留 pending entry 且重新
提交、检查 Owner 后才能推进或激活。Claim 释放 permit、重新排队同一候选；其既有
fairness META 写仍存在，待 C3 移除，不把它称为整个 scheduler 零写。

新增 9 个测试覆盖每个必需读取边界、DUE 完成而 NATIVE 耗尽、返回字节超限、
连续 elapsed 诊断、included candidate 不能掩盖存储前缀未完成、嵌套操作先写后让出、
source 重试、两次读取间 Owner 丢失、独立 dispatcher 错误、恢复 cursor/activation。
既有 Claim handoff 测试增加许可释放、候选保留和成功重试验证。

完整 `./gradlew spotlessApply check --console=plain` PASS（2m29s、37 tasks），
Java 1710 项，失败 0、错误 0、skip 41；四个直接受影响类合计 21 项零 skip。
唯一中间完整门失败为新日志行超出 checkstyle 行长，已修正并重新通过完整门。
精确源码/测试 digest、skip 清单与最终日志见 `evidence/a2-head-integration-*`。
41 个 skip 不构成真实外部服务、Broker 或恢复环境证据。

本批基于已推送并核验的 `c6e910687cbba47ed60cc6bc0de6000bd9690b10`。
A2 保持 IN_PROGRESS：旧构造器仍使用明确标注的 compatibility policy；有限配置
虽可注入并已验证失败语义，但正式装配的强制有限 policy 和最大合法单 mutation
资源证明仍待完成。按实施计划允许先推进 B1–B5 的有界契约设计来提供所需资源约束，
不得将任意测试 byte budget 或构造器的正数校验当成容量证明。全部 A–F 目标继续 active。


## 2026-09-08：B1 物理身份与基础 key 批次（切片未完成）

上一批 A2 HeadPlan/source/Claim/recovery 提交 `c3ff048b60201c984d849c92cfc68c951a808b8c`
已 push 并核验远端相同 SHA。本轮开始工作树干净，发布前 fetch 确认 ahead/behind=0/0。
A2 继续核对发现 Route maxCommandBytes 不能单独覆盖所有旧 Message/source/attempt 可变
字段；未给兼容 policy 冒充有限默认值。按允许在 A 期间推进 B 契约的计划，本批实现 B1
物理身份和基础索引布局，完整资源与 Target value 契约继续闭合。

新增 CanonicalTargetPartition/TargetPartitionId 与 TargetKeyCodec。保留完整 Broker
identity，明确 cluster/topic/完整 tuple 字节上限；旧 Lane 必须整串解析后才能提取，
Kafka 重复 UUID 冲突、Pulsar 独立 pin 冲突被拒绝。Profile、tenant、bucket、ordering
key 变化不改变相同物理 Target；不同 cluster/resource/partition 必须区分。
基础 key 预留独立 tag，slot/generation 固定放在时间之前，Native 按 deliverAt 排序。

独立 Python 标准库生成的固定 canonical/hash/key 向量与 Java 实现逐 byte 比较。
生成器默认只核对，只有显式 --write 才更新向量。基线 Lane reader 对 Store format 2
的 RocksDB 重开拒绝测试覆盖 format marker 已切换而 identity 尚未切换及两者均切换；
它不代替未来对已发布二进制的完整矩阵。尚未接入生产 writer 或开启新 Store 格式。

完整 `./gradlew spotlessApply check --console=plain` PASS（2m23s、38 tasks）。
Java 1722 项，失败 0、错误 0、skip 41；新增 12 项全部通过且无 skip。两次中间门
分别报注释版本缩写和测试未引用 try-with-resources 变量，均修正后重新通过完整门。
向量独立重算 PASS，SHA-256 为
`bd37ad15ac3e5301823f75f7e29e3658edfc15a681f249bfb6c9cbd9e51e3047`。
精确源码、未改动 reader、测试、生成器、向量 digest 和最终日志保存在
`evidence/b1-identity-key-*`；41 项外部环境 skip 不计入真实 Broker 或恢复认证。

完整主设计、Registry、ADR-0025、代码设计及实施状态同步；新增 normative
`05-Target身份与索引契约.md`。A2/B1 均保持 IN_PROGRESS：B1 仍缺完整 value/presence/
digest、严格顺序索引及激活，A2 仍缺完整最大合法 mutation 预算与正式有限配置。
全部 A–F 目标、真实 Broker/恢复、适用迁移和旧路径清退的义务均保留。


## 2026-09-08：B1 有界 Target 状态与槽位历史批次（切片未完成）

本批基于已推送并核验的 `92c451739aca189a7d685061f5e5fa547d28f594`。
发布前 fetch 确认 main 与 origin/main ahead/behind=0/0，继续单 agent、直接 main。

固定 TargetQueueState、TargetDomainState 和 TargetHeadRef 的 canonical 字段、presence、
三个独立 digest 域、非法字段拒绝及字节上限。物理 tuple 单独存不可变 identity 记录，
避免 head 热更新重写长资源字段。Store 边界核对 exact key、物理 Target、source Shard、
已激活 slot 上限，以及 Native 只能引用 Pulsar 资源；解析摘要仍不授予 Claim/发送权。

增加 ORDERED/ORDER_HEAD/ORDER_STATE key，区分业务顺序与当前 head eligibility。
queue 的 slot 历史连续保留，ACTIVE→DRAINING→VACANT 后才能以新 generation 重用；
revision 不跳号或回绕、scope 不在 bound slot 内改写，CLOSED 不重新开放。
这些只是结构校验，实际旧义务和保护排空仍需 C1/E2 gate。

schema 最多 64 slots，基础运行仍只允许一个已分配 slot。head/domain/queue canonical
保守上限为 214/587/37883 bytes，root decoder 先限制字节及总字段数。独立 Python
参考编码的 64-slot 最大宽度向量为 37626 bytes，Java 与其 SHA-256 完全一致。
固定向量文件重算 PASS，SHA-256 为
`2289d4a68d46c8eda11243db835fc3c1d526f7da719b9c98474eace5eaa8a5cb`。
有效 CRC 的 NV type 12/13 向量被当前未改动的 Lane reader 拒绝；新格式尚未激活。

完整 `./gradlew spotlessApply check --console=plain` PASS（2m28s、38 tasks）。
Java 1737 项，失败 0、错误 0、skip 41；新增状态测试 15 项零 skip，与既有身份/key
测试合计 27 项零 skip。41 项外部环境 skip 不构成真实 Broker/恢复认证。
源码、测试、当前 reader、契约与独立向量 digest 及最终日志在
`evidence/b1-target-state-*`；没有使用局部编码上限代替完整 mutation 资源证明。

主设计、Registry、ADR-0025、代码设计和实施状态同步。A2/B1 保持 IN_PROGRESS；
下一步仍需完整 work value、ORDER_STATE/消息定位、B2–B4 引用契约，以及 A2 正式有限
配置和完整资源边界。所有 A–F、真实 Broker/恢复、适用迁移和旧路径清退义务继续保留。


## 2026-09-08：B1 可逆 work 与 Message 定位批次（切片未完成）

上一批 `4d7b5d89a8a6572fbc49889b2b956a83ff6d3508` 已推送并核验远端。本批开始工作树
干净；发布前 fetch 确认 main 与 origin/main ahead/behind=0/0，继续单 agent、直接 main。

新增 TargetMessageLocator、TargetTimelineWorkRef 和 TargetSourcePosition 边界校验。
定位投影保留 Message/generation、Target/domain generation、计费、ordering 和 binding
引用，嵌入 current work 而不新增一份独立 locator key。work 保留 initial/definitive/
uncertain、attempt、runtime revision 和完整 UNKNOWN 控制 source；两路初次候选存
同一 work value，Native 不生成第二份 attempt。FIFO 业务时间与 head eligibility 分开。

跨记录方法核对 Message/queue/accounting/domain、物理 Target、source Shard/token 和
同一真实资源中晚于 Schedule 的控制 source。Native 只允许初次 best-effort，并要求
Pulsar 资源与域 policy scope；完整 binding、Owner、实时 policy、attempt/obligation
仍待 C1 在同一 plan/batch 接入，codec 不构成重试或发送 authority。

完整 SourcePosition 沿用原 wire bytes，Target 格式新增 Kafka cluster=256 UTF-8 bytes、
Pulsar topic=1MiB 和非零资源身份限制；上限分别为 318/1048670 bytes。locator/work
保守上限为 222/1049113，完整 work decoder 限制字段总数为 14。独立最大 work 向量
实测 1049075 bytes，Java 与 Python SHA-256 一致；超限旧 source 需转换冲突处理，
没有修改活动 Lane reader 的旧解码行为。NV type 14 仍只是预留，有效 CRC 向量被拒绝。

完整 `./gradlew spotlessApply check --console=plain` PASS（2m40s、38 tasks）。
Java 1750 项，失败 0、错误 0、skip 41；新增 13 项零 skip，与既有 Target 三类测试
合计 40 项零 skip。中间编译发现 envelope 测试少传 expected type，首次完整门发现
一条注释行长超限；均修正后重新通过完整门。外部环境 skip 不构成真实 Broker/恢复认证。
向量重算 PASS，文件 SHA-256 为
`dd9f9162d2a95607dd768fc477953f09a3b1d77859975a5a5bcc00f85ae5699d`；此前所有向量条目保留。
精确源码、依赖、旧 reader、测试、契约和最终日志在 `evidence/b1-work-locator-*`。

主设计、Registry、ADR-0025、代码设计与实施状态同步。A2/B1 仍为 IN_PROGRESS；
完整 Message/runtime envelope、Expiry value、ORDER_STATE、B2–B4 引用对象和格式激活
继续推进，随后完成正式有限配置和完整资源证明。全部 A–F、真实 Broker/恢复、适用
迁移和保护解除后的旧路径清退义务均保留。


## 2026-09-08：B1 完整 Message/runtime 与 Expiry 批次（切片未完成）

本批基于已推送并核验的 `205f78e2a1828d76544477a3889ea034f1bb4f1a`；开始时工作树
干净，发布前 fetch 确认 main 与 origin/main ahead/behind=0/0。继续单 agent、直接 main。

新增 TargetGenerationRuntimeIndex、TargetMessageRecord、TargetExpiryRef 和对象 payload
边界。runtime 保留 current work、累计 Admission、duplicate 标记和原 attempt refs，
终态仍可带未决 PUBLISHING/UNCERTAIN 义务；Message 只使用 runtime 的聚合状态。
检验 Message/generation/source/work/time/payload 投影，Expiry 不随 Claim/retry 刷新。

新格式最多 1024 个 refs，检查唯一排序、generation、原 key 的 ID/长度；容量须在不可逆
Admission 之前兑现，不能丢弃超限义务。inline 最多 16MiB，committed payload reference
四个变长身份字段各最多 1MiB；旧缺 proof/超限数据须从真实证据转换或列为冲突。
PayloadReference 只新增不复制字段的长度检查 API，原 wire/reader 行为未改变。

完整 runtime/Message/Expiry canonical 保守上限为 1214052/19040258/272 bytes。
1024-ref 最大 runtime 向量实际 1214014 bytes，最大 Message 向量含完整 SourcePosition
与 16MiB inline payload，实际 19040181 bytes；均与独立 Python SHA-256 一致。
id tag 05 与 NV 15/16 尚未接入活动 writer；有效 CRC 的新 type 被实际 decodeAny 拒绝，
并加强了已有 NV 14 测试，使其读取 envelope header 而不是先拒绝未知 expectedType。

完整 `./gradlew spotlessApply check --console=plain` PASS（2m29s、38 tasks）。
Java 1765 项，失败 0、错误 0、skip 41；新增 15 项零 skip，Target 五类测试合计 55 项
零 skip。首次 focused 门发现 CRC 测试以 signed int 比较 uint32 CRC，已改为完整 unsigned
读取后通过 focused 与完整门；未修改实际 CRC 或 reader 算法。外部 skip 不计入真实 Broker/
恢复认证。向量重算 PASS，文件 SHA-256 为
`de15422ab390dc68181bf76498a00dc43bbc8c2d2e5822e5d350d20cb71916e8`，此前全部条目保持原值。
源码、依赖、旧 reader、测试、契约与最终日志在 `evidence/b1-message-runtime-*`。

主设计、Registry、ADR-0025、代码设计和实施状态同步；A2/B1 保持 IN_PROGRESS。
ORDER_STATE、B2–B4 完整引用及 Target Claim/Admission/控制 mutation 契约、正式激活和
A2 全 mutation 资源证明继续推进。编码上限不等于已认证容量；全部 A–F、真实 Broker/
恢复、适用迁移及保护解除后的旧路径清退义务继续保留。

## 2026-09-08：B1 ORDER_STATE/barrier 与身份存储验收

本批基于 `adfb4d49e3db313775c7ff21cef72742520bea34`，开始工作树干净，继续单 agent、
直接 main。发布前 fetch origin/main 与本地 ahead/behind=0/0；使用既有 SSH 443 通道。

新增 TargetOrderState 和 TargetOrderBarrier。严格域保留 source Shard、execution slot/
generation、accounting incarnation、排序契约与 state/control revision；可服务 head
和未决 barrier 互斥，barrier 指向 exact Message generation/runtime revision/digest。
可逆 Claim、PUBLISHING、UNCERTAIN 和终态未决 refs 都阻止后继。ORDER_HEAD 复用 NV 14
完整 work；ORDERED 的业务顺序不随 retry eligibility 改变。NV 17 仍由旧 reader 拒绝。

保守 state/barrier 上限为 777/447 bytes，最大 state 独立向量实际 774 bytes。
旧排序 revision 1 不允许追溯水位，revision 2 显式保留 Admission watermark；codec 的
结构与 projection 检查不证明 source-ordered mutation、head 最小性或解除义务授权。
这些运行时义务继续由 C1/E5 接入，迟到拒绝/幂等由 B5 冻结，旧数据切点由 B6/F1 证明。

原设计 §17.3 的 B1 验收是身份、存储字段、独立向量和旧 reader 拒绝边界。过去子批次
把 B2–B4 引用对象、C1 mutation/激活及 B6/F1 转换也列为“B1 pending”，形成了反向依赖。
本批按原任务表在字段契约 §19 和实施计划中明确归属：B1 可以独立验收，后续 29 切片
中的义务不减少，B7 仍必须证明 B1–B6 的完整交叉契约和接受要求。旧 evidence 保留原 bytes。

新增 15 项专项全部通过。第一次 focused 门发现测试夹具误用拒绝负 long 的 u64 helper，
改为完整 unsigned bit helper；第一次完整门发现新向量名覆盖旧 `order.head.key` 和部分
if 缺大括号，分别改用独立名称并补齐。再次完整 `./gradlew spotlessApply check --console=plain`
PASS（2m39s、39 tasks）；Java 1780 项，失败/错误 0，skip 41；Target 六类测试 70 项零 skip。
全部 75 条旧向量原值保留，新增 23 条；向量 SHA-256 为
`e97a8734bc44f3da63cd55f6f5eb0faa899a6d01e61bf4991b0bf5020db51330`。
新增 `checkTargetIdentityVectors` 已接入常规 check，执行只读独立校验。

主设计、Registry、ADR-0025、代码级设计和实施状态已同步，B1 改为 VERIFIED，精确源码、
契约、构建、测试、历史批次与新日志见 `evidence/b1-results.json` 和 `b1-order-state-check.log`。
外部 skip 不计入真实 Broker/恢复认证。A2 仍未完成正式有限预算装配与合法 mutation 上限
证明；接下来推进 B2 的执行/控制兼容与绑定定义，并继续完成 A2/A3 的独立活动路径验证。
Target Store 未激活、NDIP-3 仍为 Draft、productionAuthority=false，完整 A–F 目标保持进行。


## 2026-09-08：B2 执行/控制兼容与有界注册规划（切片未完成）

本批基于已推送的 `ec4e94623aa4b3eafa7e2ea6a14c8ce28a871afc`；开始时工作树干净。
继续单 agent 直接 main，未创建 worktree、分支或子任务。发布前 fetch 已确认远端
main 与本地基线 ahead/behind=0/0。

新增 TargetDispatchCompatibility、TargetControlScope 与 bounded codec，固定真实
执行保证和完整额外控制/permit 集合。Profile identity/version、retry、payload、bucket
不直接拆域；fixed offered contract 可以满足较弱的保留期/timing 要求，认证 producer
上限的覆盖方向受控。scope 差异仍拒绝共队，DTO 不充当完整成员或 credential 授权证明。

TargetDomainRegistration 是纯规划器，核对全部非 VACANT 的完整引用，优先匹配
ACTIVE，拒绝只剩 DRAINING 的匹配，递增 VACANT generation 且禁止 UINT64_MAX
回绕。Plan 绑定 queue revision/digest。基础 max slots=1，至多 64 的扩展布局只作
E2 预留；新增五个稳定拒绝码均为 NEW_PREPARATION_REQUIRED。

meta tag 0c/0d 与 NV 18/19 尚未激活，旧 Lane reader 对有效 CRC 也拒绝。dispatch/control
的 canonical 保守上限为 1049187/2396 bytes；独立最大向量实际 1049181/2396 bytes。
新增 Python 生成器的 31 条独立向量通过常规 check 只读验证，SHA-256 为
`9c4189edbdcc3ab31314843ac714ee74995dafbc4b1775953a650ef6b51f2fdc`；B1 的 98 条
向量及其生成器保持原 bytes。

专项命令 `./gradlew spotlessApply test --tests '*TargetCompatibilityContractTest'
--tests '*ProtocolCodecTest' --console=plain` PASS（12s）：B2 20 项、协议 48 项，均
无失败/错误/skip。首次完整 check 的 Java 1800 项、失败/错误 0、skip 41，但一个
新增 Javadoc 的行长检查失败；已拆行并重新执行完整门。最终通过结果及精确文件
摘要由 `evidence/b2-compatibility-results.json` 和最终 check 日志记录。
最终 `./gradlew spotlessApply check --console=plain` PASS（3m2s、40 tasks）；Java
1800 项、失败/错误 0、外部 skip 41；B2 20 项与 B1 70 项均零 skip。

主设计、Registry、ADR-0025、工作包规范/计划/代码设计与实施状态同步。B2 为
IN_PROGRESS：source-bound group membership/common credential authority、完整
TargetScheduleBinding、通道身份与旧 attempt 冻结、共享请求归属/teardown、完整能力
支持表仍待本切片完成。C1 原子 source/Owner/Result/SourceAdvance、C2 通道池、B3
Native、E2 K 域 runtime、B6/F1 转换均保留。外部环境 skip 不认证新 Broker/恢复路径。


## 2026-09-08：B2 精确 Schedule/Prepare binding 与不可变通道（切片未完成）

本批从已推送且工作树干净的 `94ebdd48588e81b6e097c4ca4198fb8c68095c34` 开始，
继续单 agent 直接 main。新增 TargetScheduleBinding、TargetScheduleBody 和
TargetChannelIdentity，未改旧 ScheduleBinding、ChannelResourceIdentity 或活动 reader。

binding 保存完整 Registry Schedule/Prepare body、授权 source、required/offered/control/
membership refs 及 domain/accounting/ordering/Native 投影。Prepare source 与 Commit
source 分离，原 bytes 与 legacy FORBID 默认不改写；集合和字节 preflight 在旧 decoder
分配列表之前完成。channel 保持 stable producer/transactional/sequence 域，续期形成
新的完整 lease/holder scope，旧 attempt 必须使用精确冻结 identity。Store 读取额外
核对 source Shard，新工作另验 OPEN/ACTIVE 和已激活 channel slot 上限。

完整 binding/channel 的保守上限为 20976072/1279 bytes，最大独立向量实际为
20972671/1274。独立 Python 生成器输出 41 条新向量，SHA-256 为
`65b6405f9b70a96b98c7597ae25ae57a3be4d9e75cc16ed4b5fca30c59d697e9`；B1 98 条和
B2 兼容对象 31 条向量及其生成器保留原 bytes。新只读向量任务纳入常规 check。
meta 0e/NV 20 和 id 06/NV 21 尚未激活；有效 CRC 仍被旧 Lane reader 拒绝。

专项命令 `./gradlew spotlessApply test --tests '*TargetBindingChannelContractTest'
--tests '*TargetCompatibilityContractTest' --console=plain` PASS（15s），新增 18 项加上
已有 B2 20 项均零失败/错误/skip。首次编译补齐 CommandType 的显式 closed switch；
初始测试修正 generation 耗尽异常类型、optional Native presence 及缺失 digest 的
测试 helper。首次完整 check PASS（2m30s）；随后补上 NV type 与独立 envelope 的关联
断言并再跑完整 check。Java 1818 项，失败/错误 0、外部 skip 41，Target 八类 108 项零 skip。
最终精确源、依赖、规范、测试和日志绑定在 `evidence/b2-binding-channel-results.json`。

工作包新增规范 07，主设计、Registry、ADR-0025、计划、代码设计及状态同步。规则表
区分 Owner loss、Producer fault、逻辑 refcount、physical close 与 durable reference
retirement；声明四类 managed 能力与 Native/旧路径边界。B2 仍为 IN_PROGRESS：
membershipGrantRef 的完整对象、source-bound group completeness 和共同 credential/
guarantee authority 尚需闭合。实际原子 source/Owner/Admission、通道池、teardown、
Broker/恢复、适用迁移与旧路径清退仍按 C/D/E/F 推进；A2 的完整预算装配义务保留。


## 2026-09-08：B2 完整 membership grant 与 source 快照（切片未完成）

本批基线 `69ac8ca85a0b69415b19e2c40e28e1728a18eaf6`，远端一致、工作树干净，
继续单 agent 直接 main。实现 TargetMembershipGrant、TargetMembershipAuthority 与
TargetMembershipAuthorization；pre-append 注册字段不需要未来 source offset，
source apply 后补入实际 mutation digest 和完整物理位置，消除自引用。

完整 grant 包含 tenant/Profile、required/offered/control 原文、策略与操作引用及
source 身份，静态校验覆盖授权记录完整性、首次绑定关闭和 Profile 激活、精确 domain
plan、共享 provider 与旧 identity 保护。注册和完整记录保守上限 2101194/3149936 bytes；
独立最大记录为 2101182/3149924。新增 17 条 Python 向量，fixture SHA-256 为
`51de0822a7447ee89095c4c461f3302d429f20b239bd7f85a521467c525a65b9`。
meta 0f / NV 22 仅预留，未修改旧 reader、Store format、ScheduleBinding、ChannelResourceIdentity
或 CredentialUseLease。所有旧 fixture/generator 保留原 bytes。

先运行 membership/compatibility/binding-channel focused 回归。首轮测试编译修正既有
API 名称（Field.rawValue/unsignedValue、credentialBindingDigest、BrokerResourceIdentity.pulsar）；
独立向量暴露旧 source helper 默认 append time 不一致，新生成器显式固定该输入为 90。
随后 50 项专项 PASS（13s），再补一项 forged plan 的 CLOSED/DRAINING/活动槽覆盖回归。
最终完整命令为 `./gradlew spotlessApply check --console=plain`，日志及精确结果分别见
`evidence/b2-membership-check.log`、`evidence/b2-membership-results.json`。

同步规范 08、README、计划、代码设计、主设计、Registry、ADR 和状态。B2 继续完成
认证发放/关闭的 Control/SystemMutation wire、策略权威对象与整体验收；当前 source
seam 不能冒充这些实现。后续 C1/C2 原子写入和实际通道池、D/E/F Broker/恢复/迁移与
受保护旧路径清退全部保持必做，A2 的完整 finite mutation 装配继续保留。

本批最终完整 check PASS（2m45s，42 tasks），Java 1831 项、失败/错误 0、外部 skip 41；
Target 九类契约 121 项零 skip。结果行回填后仅重跑文档/版本门，日志保存为
`evidence/b2-membership-final-docs-check.log`；最终 receipt 绑定回填后的文档与此补充检查。


## 2026-09-08：B2 认证成员策略/Control 与原切片验收

本批基线 `e1feb9c5817ea20c43a9d4d6df13965d69df3f98`，开始时远端一致且工作树干净；
单 agent 直接 main。新增完整 TargetMembershipPolicy、ControlRequest/Body 和
TargetMembershipControlVerifier，并接入已有 Prepared、目标登记、角色授权与签名工厂。
Policy 的 tenant/Control scope、完整 offered/control、共同 provider mode 与 exact
Profile/required ref 全部固定；自报 hash 不构成审批。source verifier 要求两层 Ed25519、
精确已登记对象、actor/角色/资源 proof、完整审批原文和 source 关联，关闭保留历史位置。
它不访问成员私有 credential head，不写 Store/Result/SourceAdvance。

新增 meta 10 / NV 23、Control op 16/17、ApplyShardControl kind 15/16；旧 reader/DelayShard、
format 1 和 NV 1..11 未改。真实本地临时 RocksDB 验证旧 source 路径持久拒绝合法签名
的新 grant mutation，重开后保留 REJECTED / STALE_SYSTEM_MUTATION，未安装 grant/policy。
这项测试仅证明未激活边界。没有启动新的 Broker 环境或认证真实 Oxia/审批后端。

新增 34 条独立 Python 向量，fixture SHA-256 为
`83dfbdf7d4175b9574a0ef7ffeac769f39a1287fcbe7a1656ecb68b8415797ee`。
policy/request/body 保守上限为 1052039/3153314/3153474 bytes，最大合法独立输入分别
1052033/3153224/3153378 bytes。旧四组 fixture/generator 及历史 receipt 保留原 bytes。

签名/角色/策略 focused 回归通过，随后补足 author 替换、UINT32MAX 公钥版本和后端
异常传播，再加入本地 Store 重开拒绝测试；其首次编译修正为已有 ApplyStatus 和
applyStatus() API。最终命令 `./gradlew spotlessApply check --console=plain` PASS
（2m31s，43 tasks）。Java 1846 项，失败/错误 0、外部 skip 41；新增 15 项及全部十类
Target 契约 136 项零 skip。完整日志为 `evidence/b2-membership-control-check.log`。

新增规范 09，并同步 06/07/08、主设计、Registry、ADR-0025、实施计划/代码设计及状态。
按原 §17.3 对可共队矩阵/ref、基础 1/E2 K 布局、注册拒绝/稳定码、A 阻塞/B 健康、
新绑定/存量冲突、通道冻结/teardown、支持矩阵七项逐项核验，B2 记为 VERIFIED。
该状态限于原 B2 设计与字段验收；生产部署不是其前置。C1 必须实现真实权威后端及历史
审批保留、一致受保护快照、去重和有限预算原子提交；C2 实际池/回收，B3–B7、D–F 及
A2/A3 原有未决继续保留。下一项为 B3 共同 Native policy，全部 29 项目标保持 active。

结果/状态回填后仅重跑文档与版本门，日志为
`evidence/b2-membership-control-final-docs-check.log`。最终 `evidence/b2-results.json`
绑定最终源码、所有规范/同步文档、独立输入、检查日志和逐项验收；不改历史批次结果。


## 2026-09-08：B3 共同 Native 策略、独立签名与原切片验收

本批基线 `f159642c85612c95057f3550065dde6fded9d545`；开始时工作树干净、main 与已推送
origin/main 一致。继续单 agent 直接 main，实现七个 Target Native 契约/校验类型：
ArtifactSet、PolicyScope/Snapshot/Head、PolicyTrust/Authority、PolicyChecks。scope 绑定
source/Target/domain/accounting、独立 namespace/Control scope、dispatch/control、固定
cap 和完整新 artifact；不同 Profile 可以共享共同授权，不能仅因相同 cap 获得许可。

snapshot schema=2 和独立 Ed25519 签名域拒绝旧 Profile policy 重解释；publisher 必须
有 exact source-approved permission、双角色/范围证明、合法 key 和有限 lease。历史
Activation 固定完整签名 snapshot，MemberApproval 独立批准共同 Native，关闭不增加
Profile 热路径开关。CAS 保留 exact +1 generation、不回绕和全部已授予 lease 高水位。
普通 due 独立，SHADOW 只报告 would-eligible，Admission 二次 current read 后冻结，
ownership 前保留历史 trust/lease；ownership 后继续原 UNKNOWN/Journal 义务。

reserved meta 11/NV 24、meta 12/NV 25，旧 reader、Handoff/ArtifactGenerationSet、活动
Lane source/Store format 及此前五组独立 fixture/generator 保留原 bytes。Scope/artifact/
snapshot/head 上限为 397/121/646/695 bytes；独立最大输入为 394/121/644/693 bytes。
新 Python 标准库生成器先验证 RFC 8032 test 1，再生成 38 条项目向量，不读 Java 输出；
fixture SHA-256 为 `e90027f6042b8bb0086efac26a571d25e1f56ceecb0346fac3665cf12d81df03`。

20 项新增专项及 Target membership/binding、旧 Handoff 回归通过（15s）。首轮 strict
Native 测试调整为断言既有 CanonicalScheduleIntent 在 binding 构造前拒绝，未改协议
规则。首次完整 check 的功能测试无失败，但 Checkstyle 报一个 121 字符 Javadoc 和
测试 helper 的 11 个缺少大括号；修正后重跑 `./gradlew spotlessApply check --console=plain`。
最终完整 check PASS（2m27s，44 tasks）：Java 1866 项、失败/错误 0、外部 skip 41；
Target 十一类共 156 项零 skip。日志为 `evidence/b3-native-policy-check.log`，所有 JUnit
XML 以固定 ZIP 元数据保存到 `evidence/b3-junit.zip` 并逐文件比对内容。

新增规范 10，并同步主设计、Registry、ADR-0021/0025、计划、代码设计和状态。按原
§17.3 B3 的 scope/旧 Profile 收敛、新签名/发布权限、固定 cap/回退、向量/Mode 表、
trust/Admission 时序五项验收，对应源码与测试逐项记录；B3 记为 VERIFIED，仅覆盖
该原始设计冻结范围。生产 backend/source/Claim/Admission/ownership 与有限资源装配
仍由 C1/C2 完成，A2/A3、B4–B7、C–F 的全部实现、Broker/恢复、迁移清退义务保留。
没有启动新的 Broker/真实 Oxia 环境，也未将本批视作 production authority。

结果/状态回填后运行 `./gradlew checkDocumentation checkProjectVersionMarkers --console=plain`，
日志保存到 `evidence/b3-final-docs-check.log`；最终 `evidence/b3-results.json` 绑定最终
源码、规范、独立输入、检查/归档和五项原始验收。下一项为 B4 quota，整体目标保持 active。

## 2026-09-08 B4：独立 counter、局部 revision 与增量运算基础

基线 `2b31978da4929af7e6775e206ace5b3ac40e34e0`，单 agent/main。恢复工作时工作区干净，
HEAD 与 origin/main 相同。先按原 §11.1、§16.6、§17.3 审核当前 DelayShard 和
LaneQuotaUsageProjection：正常更新仍复制全部 entries、同步 revision 并重算 aggregate。
本批新增五个 protocol 对象和一个纯 runtime planner，预留 counter NV 26/meta 13、
aggregate NV 27/meta 14；当前 writer/旧 reader 未改。原 CapacityVector 16/17 仍是
Lane 维度；Target/domain/strict-domain/incarnation 使用新字段，完整 grant 关联继续
留在 B4 的未完成列表中，未把该设计义务转交给 C4 猜测。

新增 `TargetQuotaDelta` 通过有正数上限的完整 identity 单点查询生成局部 prior/next；
aggregate 只计算主费用。局部 revision、SourcePosition/sequence、提交前旧 bytes、
raw uint64 溢出、净零转移、tenant 镜像和独立账本审计边界形成可执行检查。
保持无 Store 写/无提前修改共享 Map 的纯接口；未声称实际提交、写不确定性恢复已验证。

新增 Python 标准库独立编码器与 24 条 fixture 项，包括四种 identity/key、usage、
mutation、counter/aggregate/genesis 和最大样本 length/SHA-256。新增 21 项 Java 测试，
包含 L=1/100/10000 时单点 lookup 次数均为 1；这只测 planner，不是 Store 或端到端成本。
第一次 focused 编译因测试使用未限定的嵌套 EntryKind 失败；修正为
PulsarSourcePosition.EntryKind 后执行 `./gradlew spotlessApply test --tests '*TargetQuotaContractTest'
checkstyleMain checkstyleTest --console=plain`，PASS（21s，21 项、失败/错误/skip 均为 0）。

最终 `./gradlew spotlessApply check --console=plain` PASS（2m29s，45 tasks）：
Java 1887 项、失败/错误 0、外部环境 skip 41。完整日志保存为
`evidence/b4-counter-foundation-check.log`，focused 日志为
`evidence/b4-counter-foundation-focused.log`，全部 297 份 JUnit XML 固定 ZIP 元数据归档。
工作包 11、原设计、Registry、ADR-0025、主设计、代码设计、计划和状态同步。

B4 保持 IN_PROGRESS：完整逐业务 delta 表、唯一 byte/record 来源、grant/计量 artifact、
tenant slot 来源和 incarnation 分配/退休保护继续实施。没有运行新的真实 Broker/Oxia/
恢复环境。C4 的活动 writer 替换、业务/结果/source 同批写、内存发布和真实账本重建，
D/E 的外部环境与故障恢复、F 的适用迁移/旧路径清退均保留。整体 29 项目标未完成。

状态回填后运行 `./gradlew checkDocumentation checkProjectVersionMarkers --console=plain`，
最终日志为 `evidence/b4-counter-foundation-final-docs.log`。精确源码、规范、旧文件不变
验证、独立向量、完整测试和归档由 `evidence/b4-counter-foundation-results.json` 绑定。

## 2026-09-08 B4：固定计量与 attempt reserve 转实占

基线 `c9bff4125f225f7d9ef077957c24806cad30a7e9`，单 agent/main；工作区和 ancestry
重新核验后继续。审核主设计 §18.1/18.2、既有 charge vector 与 Outcome release 路径，
区分逻辑 attempt、未使用 reserve、retained record、唯一 payload owner 和 physical/zombie。
本批新增 TargetQuotaAccounting 与 TargetQuotaAttemptBudget；后者预留 NV 28/meta 15。
它们尚未接入活动 source/Store writer，当前 Lane quota、既有 grant 与旧 reader 未变。

固定 artifact 编码覆盖 schema bundle/input version、record/adapter overhead 与 minimum
cost；费用来自冻结 canonical lengths。NV records 明确计入 12-byte envelope，WAL
接受完整 frame；一个 record 仅属于一个 record-byte class。Attempt budget 保留完整
locator、tenant、Admission hash、artifact/lineage、commitment/allocated 和 source stamps。
UNKNOWN 不释放费用；确定 Outcome 释放 7/8，unused reserve 等最后 reserved writer 被
Floor 包含后经 ReleaseAuthority 转 retained，实际 GC 再逐维释放 allocated。

首轮转换测试通过后继续审查，发现 per-attempt reserve 不能包含共享 retained payload
维度 4，已收紧为 3/9–15 并新增拒绝向量。补充了 resolved writer 的后续 source allocation
及 Floor 必须覆盖最新 allocation 的检查，避免只用最初 resolution 过早减 reserve。
错误/authority fatal Error 均不产生替换预算；测试权限函数不作为实际 catalog/pin/删除证明。

新增 30 条独立 Python 编码项和 19 项 Java 测试。最终专项命令：
`./gradlew spotlessApply test --tests '*TargetQuotaAccountingContractTest' --tests '*TargetQuotaContractTest'
checkstyleMain checkstyleTest --console=plain`，PASS（22s，40 项、失败/错误/skip 0）。
完整 `./gradlew spotlessApply check --console=plain` PASS（2m35s，46 tasks）：
Java 1906 项、失败/错误 0、外部环境 skip 41。日志为 `evidence/b4-accounting-check.log`，
专项为 `evidence/b4-accounting-focused.log`；全部 298 份 JUnit XML 固定元数据归档。

§11 契约补齐计量、预算字段/阶段表和原 §17.3 完整业务 delta 表；同步原设计、Registry、
ADR-0025、主设计、代码设计、实施计划和状态。B4 保持 IN_PROGRESS：Target grant 的
认证激活、所有 record 的唯一费用来源、counter/budget bookkeeping 与有限 reserve sizing、
非 attempt payload/identity 保护、incarnation 分配/退休仍须闭合。本批不声称 C4 真实
原子提交/释放后端、Broker/恢复或 F 迁移清退已完成，完整 29 项目标继续。

回填后运行 `./gradlew checkDocumentation checkProjectVersionMarkers --console=plain`，
最终日志为 `evidence/b4-accounting-final-docs.log`。`evidence/b4-accounting-results.json`
绑定最终源码/规范、独立向量、旧路径及上一批基础不变校验、完整测试与归档。


## 2026-09-08 B4：跨 incarnation 总额与完整 grant artifact

本批从主设计 §5.4 核实一条 Ingress Route 只绑定一个 tenant Security Domain；
Source Shard 的 grant 因而继续使用既有 primary aggregate，包含该 Route 的共享
source-local metadata。没有把 TENANT_SHARD 重新解释为所有 Target 的汇总。

新增稳定 TargetQuotaScope（无 incarnation/Profile/domain）、跨 incarnation
TargetQuotaTotal（NV 29/meta 16，固定 key 87 bytes）及完整 TargetQuotaGrant artifact。
当前、历史 incarnation 的 primary 费用共同占用一个 Target 总额；tenant mirror 和
派生 total 均不重复加入 shard aggregate。完整 source/grant authority 激活未实现。

TargetQuotaTotalsDelta 从已有 immutable leaf delta 派生有限变化集合；每个受影响
Target 一次 point lookup，先减所有 prior 再加 next。普通单 Target Schedule 的 quota
部分显式增加一个 total write。净零转移仍更新 total stamp，无变化/mirror-only/
SHARD-only 不更新 total。Composite guard 校验完整 prior bytes，恢复投影审计核对
所有 incarnation，禁止正常 mutation 全量重建。Total 必须覆盖每个 primary leaf，
且被 shard aggregate 覆盖；匹配的 revision/source 不能掩盖层级少计费。

TargetQuotaGrantGate 区分首次 Schedule/Prepare/DLQ Replay 与已有工作。前者比较
两个完整 primary 总额及计量 artifact；后者不因逻辑 grant 下调阻断合法 Claim/
Admission/Outcome/terminal 等。该返回值不替代真实 ledger、grant source 激活、
Outcome reserve、target execution 和物理/zombie pool gate。

验证命令与结果：

- `./gradlew spotlessApply test --tests '*TargetQuotaScopeContractTest' --tests '*TargetQuotaContractTest' --tests '*TargetQuotaAccountingContractTest' checkstyleMain checkstyleTest checkTargetQuotaScopeVectors --console=plain`：23s，新增 23 项、共 63 项 quota 专项通过，无 skip。
- `./gradlew spotlessApply check --console=plain`：2m33s，47 tasks；1929 Java tests，失败/错误 0，外部环境 skip 41。299 份 XML 全部归档。
- 全部 1145 个源码/测试/向量/构建输入在本次完整检查前后 hash 相同；fixture 有 18 个独立条目，SHA-256 为 `d5a93d1296a7775d3b489d5475df25b1fcf7f826ff431e6b3fa07c9b0c63360a`。
- 证据：`evidence/b4-scope-results.json`、`b4-scope-check.log`、`b4-scope-focused.log`、`b4-scope-junit.zip`；旧 counter/accounting/attempt foundation、Lane writer 和旧 NV reader 源码保持逐字一致。

初次完整检查通过后，复核补充了 parent 覆盖关系及排空后 total revision 不重置的
测试，随后再次完成上述专项和完整检查；这里引用的是最终源码的结果。测试只证明
闭合编码、本地算术/策略与投影守恒，不证明实际 Store 原子提交或 Broker/恢复认证。
B4 保持 IN_PROGRESS，继续完成 grant authority/source control 契约、每类 record 的
owner/bookkeeping 与有限容量、incarnation 分配/回收与 legacy handover。A–F 全部
29 个切片的目标仍 active，C4/D/E/F 的实现、真实环境、迁移和受保护清退义务保留。

## 2026-09-08 B4：认证 quota grant Control 与 source 激活契约

基线 `main@a3735fd0aebf4f3150d4f3bbc96f9882a8870669`。本批注册独立
PUBLISH_TARGET_QUOTA_GRANT=18 / APPLY ControlKind=17，携带完整 next/prior artifact、
可选父 transfer ref，绑定唯一 SHARD index 0 的 expected mutation ID/hash。Outer field
14 始终 absent，raw prior version 只在完整 request 内编码，不改变旧 scalar 的语义。

NV 30/meta 17 保存完整 request、ControlRef、首次 source/sequence、accepted ID/hash，
stamp 使用完整签名 canonical envelope SHA-256。Shard/Target key 为 55/87 bytes；
只嵌入 prior grant，不递归嵌入 prior activation。独立 Python Ed25519 向量同时覆盖
初始/更新/transfer/zero、两种最大 branch、raw uint64 和 1 MiB Pulsar source。

首次应用 verifier 核对 source 有效的双签名、registered actor/role/resource、exact
prepared bytes、Route、完整 prior、source 顺序及 Store read set，然后调用强制
CapacityAuthority。后者获得完整 body/ControlRef、View 和实际 SourcePosition，必须
证明 tenant policy/static cuts、grandfathered usage、physical reserve 与父 transfer
顺序，快照须保持到原子提交。该接口没有生产后端；返回 Change 不执行 Store 写。
拒绝、transient exception 与 fatal Error 均不能返回可发布额度。下调到零不重写旧费用。

验证结果：

- `./gradlew spotlessApply test --tests '*TargetQuota*Test' --tests '*TargetMembershipControlTest' --tests '*PreparedControlOperationTest' --tests '*ControlRegistrationBindingTest' --tests '*ControlRequestSupportCodecTest' checkstyleMain checkstyleTest checkTargetQuotaGrantVectors --console=plain`：22s，8 个测试类、113 项，无失败/错误/skip。其中新增 grant control 23 项，quota 总计 86 项。
- `./gradlew spotlessApply check --console=plain`：2m28s，48 tasks；1952 项 Java tests，失败/错误 0，外部环境 skip 41。300 份 XML 全部归档。
- 1158 个源码/测试/向量/构建输入在最终完整检查前后 hash 相同。74 条独立向量的 fixture SHA-256 为 `8d044bd468c0efa8c31e7ff7382f44d10f73f5a5134d9eba01dadcfec87c8936`；所有依赖的旧 Target generators 及旧 fixtures 保持逐字一致。
- 当前 Lane reader 拒绝新 kind；实际临时 RocksDB DelayShard 返回 REJECTED / STALE_SYSTEM_MUTATION，未写入激活 key。关闭重开后拒绝结果及 key 不存在仍成立。此测试不代表新 Target Store 接受、恢复或 Broker 认证。
- 首次完整检查暴露旧 support test 把 18 当作未知 Control kind；已改成确认已注册 18 并拒绝 19，上述专项及完整检查均为修改后的最终结果。

`evidence/b4-grant-results.json` 绑定精确源码、输入、文档、日志及 `b4-grant-junit.zip`；
旧 Scope/counter/accounting/attempt foundation、Lane writer、旧 NV/APPLY readers 与旧
SystemMutation 编码逐字不变。主设计、Registry、ADR-0025 和工作包均已同步。

B4 保持 IN_PROGRESS，下一批继续完整 record owner/bookkeeping、有限容量、tenant/domain
唯一计数和 incarnation 分配/回收/legacy handover。C4 仍须完成真实 authority、Result-first
dedupe、activation/bookkeeping/Result/SourceAdvance 同 batch 和提交后发布，以及真实
ledger 重建与不确定提交恢复。A–F 全部 29 个切片目标继续 active。

## 2026-09-08 B4：accounting projection 自身的固定存储预留

基线 `main@fa99bcaa32c053b64ea5fab1b948eb9fd78789bf`。新增
TargetQuotaBookkeeping（NV 31/meta 18，key 22 bytes），绑定完整 SHARD root、tenant、
冻结计量 artifact、counter/total/activation 三类记录数、独立 revision/source 与 digest。
Root incarnation 与实际 aggregate 一致；source/sequence 同序，同位置须完整 stamp
一致。Anchor 缺失或 genesis 不构成恢复默认值。

Root 以各类固定 schema/key 上限为 counter/aggregate/total/activation/anchor 预留
存储；把全局 SourcePosition bound 替换成该 immutable physical Route 的固定宽度，
Kafka 始终预留可选 epoch。无需递归编码自身 usage；value 原地更新不新增槽位。
Root primary 与 mirror 是两条物理记录，但其总预留只通过 primary 计入 aggregate。
退休 counter、零 total/zero grant 在实际删除前继续收费。Attempt budget 记录使用它
自己的冻结 Target owner/artifact，在 commitment/allocated 之外唯一计费，RELEASED
仍保留该记录费用。其它业务记录 owner 与释放权限未由此自动完成。

检查结果：

- `./gradlew spotlessApply test --tests '*TargetQuota*Test' checkstyleMain checkstyleTest checkTargetQuotaBookkeepingVectors --console=plain`：18s，5 个测试类、105 项，无失败/错误/skip；新增 bookkeeping 19 项。
- `./gradlew spotlessApply check --console=plain`：2m36s，49 tasks；1971 项 Java tests，失败/错误 0，外部环境 skip 41。全部 301 份 XML 归档。
- 1162 个源码/测试/向量/构建输入在最终完整检查前后 hash 相同。45 条独立向量，fixture SHA-256 为 `4a8dbc4fab6da308445e1b889395c3d4f591786b22a4db62b58afb8637aefdc8`。
- 向量与测试覆盖 Kafka/Pulsar/epoch/多投影编码、1 MiB source/raw revision、UTF-8 和实际 counter/total/aggregate/activation/budget 编码覆盖；重复/缺失/foreign inventory、root/incarnation/stamp 不一致、乘加溢出、退休记录保留及五阶段预算记录费。
- 旧 reader 拒绝合法 CRC 的 NV 31。此前计量/attempt/total/grant/control 及活动 Lane writer 的 62 个保护文件逐字不变；此批没有新 Target Store 原子写或真实 Broker/恢复认证。

`evidence/b4-bookkeeping-results.json` 绑定精确源码、文档、日志、旧基础保护和
`b4-bookkeeping-junit.zip`。主设计、Registry、ADR-0025 与工作包已同步固定公式、记录
生命周期和 C4 原子/审计责任。编译期间修正了测试工具名、私有常量访问和格式问题；
这里记录的全部结果均来自最终源码。

B4 继续 IN_PROGRESS：下一步闭合其它业务记录 owner 与 before/after ledger、非 attempt
的 payload/identity 保护、完整业务 reserve sizing、tenant/domain cardinality 和
incarnation 分配/退休/legacy handover。C4 必须实现真实 inventory 枚举、guarded atomic
batch、post-commit 发布、受保护删除与不确定提交恢复。全部 A–F、29 切片目标继续 active。


### 2026-09-08：B4 唯一 Message payload owner 契约

基于 `d56bb366dac2a2e3d1502e610fe74a39730434ab`，直接在 main 继续本切片。
新增 `TargetQuotaPayloadOwner`（NV 32/meta 19，43-byte Message ID key），保留原始
TARGET/incarnation、tenant、完整 accounting 与初始 binding，跨 DLQ generation 不再
分配另一份 payload 费用。Reservation commit/expire、active retention、DLQ replay、
Floor release 通过强制 transition authority；完整 binding/source/object/Message 检查
拒绝同位置元数据差异、未提交对象伪装 inline 和无 commit 的 reservation replay。

Owner record 按实际 canonical bytes 单独计 STATE，RELEASED 仍持有记录费用直到受
保护实际删除。最大结构样本同时包含 1 MiB Pulsar source、四个 1 MiB object component
和 raw uint64 最大 revision；独立 Python 验证长度、摘要和费用，不把结构样本当作
已认证 source trace。文档明确 literal 历史副本仍归各自 record/WAL 类，不用 payload
释放宣称历史 bytes 全部物理抹除，也不允许绕过 fetch/replay 授权。

验证：

- `./gradlew spotlessApply test --tests '*TargetQuota*Test' checkstyleMain checkstyleTest checkTargetQuotaPayloadVectors --console=plain`：6 类、121 项，失败/错误/skip 0；新增 payload 16 项。
- `./gradlew spotlessApply check --console=plain`：1987 项 Java tests，失败/错误 0、外部 skip 41；全部 302 份 JUnit XML 归档。
- 1166 个源码/测试/向量/构建输入在最终完整检查前后 hash 相同。38 条独立向量，fixture SHA-256 `cec01f373d11c9a1c46f129eb2d71d8933434011c95962e675480250b2188ccd`。
- 66 个旧计量/attempt/total/grant/control/bookkeeping 与活动 Lane 路径保护文件逐字不变；旧 NV reader 拒绝新格式。
- 最初完整检查发现文档中的协议版本简写触发项目命名门禁，改为明确 wire version 数值后重新通过；没有改变编码或放宽检查。

`evidence/b4-payload-results.json` 绑定源码、完整输入、文档、日志、旧基础保护与
`b4-payload-junit.zip`。B4 保持 IN_PROGRESS；完整 metadata/outcome reserve、其它
业务记录 owner/实际 ledger、tenant/domain unique cardinality、incarnation 分配/退休和
legacy handover 仍须设计闭合。C4 的实际 source/receipt/transition/delete 后端、原子
Store 与独立恢复，D/E 的真实 Broker 和 F 的受控迁移/旧路径清退均未被本批替代。


### 2026-09-08：B4 Message 家族实际记录计费来源

基于 `7964b8e2d0521521aee347b4fb08a567d2848852`，直接 main 实施。
`TargetQuotaMessageRecords` 连接冻结 payload owner 与已有实际 record decoder：
owner、initial binding、Message、DUE/NATIVE/ORDERED/ORDER_HEAD、Expiry 共八种角色。
每个真实 key 按完整 typed bytes 单独计 STATE，Native/ordinary 两个副本分别付费；
Message 内嵌 runtime 和共享 state 内嵌 head/barrier 不伪造独立 record。

完整 key/value 校验后返回不可从外部构造的 immutable Record，提供实际 CF/type/key/
value 重验。固定最多 6 条同一 owner/Message 视图的子集，拒绝重复和混合 before/after。
ORDER_HEAD 必须有实际 OPEN/serviceable/no-barrier state；终态 Expiry、claimed stale
work、物理 source 替换及同位置元数据差异均失败关闭。Release 后仍存在的 owner 与
initial binding 继续付 STATE；partial list 不证明未提供记录不存在。

验证：

- `./gradlew spotlessApply test --tests '*TargetQuota*Test' checkstyleMain checkstyleTest checkTargetQuotaMessageRecordVectors --console=plain`：7 类、135 项，失败/错误/skip 0；新增 14 项。
- `./gradlew spotlessApply check --console=plain`：2001 项 Java tests，失败/错误 0、外部 skip 41；303 份 XML 全部归档。
- 1170 个源码/测试/向量/构建输入在最终完整检查前后 hash 一致。25 条独立 Python 向量，fixture SHA-256 `99893997df03a4d34425fd9637af8314edd82a6fc282bdb2e88f30d88487b26f`。
- 71 个此前 quota/grant/bookkeeping/payload owner/活动 Lane 保护文件（含 TargetKeyCodec）逐字不变。没有新增 key/NV discriminator，也未激活 Target writer。

完整 evidence 与源码、文档、日志、保护文件、输入、JUnit archive 绑定于
`evidence/b4-message-records-results.json`。规范 §14 给出八角色完整计费矩阵与原 §9
各动作的真实 before/after 来源；C4 必须在实际 Store/WriteBatch 和完整 read-set guard
下执行，不能以此 helper 的子集代替全部 ledger 或 source/grant/delete authority。
B4 继续完成其它共享/Claim/result 记录族 owner、完整 reserve、unique cardinality、
incarnation 与 legacy 生命周期；C4/D/E/F 的生产后端、原子恢复、真实 Broker、受控
迁移和旧路径清退仍按原目标执行。


### 2026-09-08：B4 Source-derived incarnation 与局部 cardinality

基于 `d676cff431e530efbb6cafc16cac64d557836dad`，单 agent、直接 main 继续。
新增 `TargetQuotaIncarnation`（NV 33/meta 1a，39/71-byte key），完整 scope/accounting/
lineage/首次 allocation mutation 派生 16-byte primary incarnation。保留完整 origin 与
可选一次 drain source；StateAuthority 必填，算术溢出在它被调用之前拒绝。

Descriptor 自身保留固定两 Source 槽位 STATE 与唯一 incarnation=1；tenant 不重复计
incarnation。QueueState 的完整 key/value 提供一个 local Target、ACTIVE+DRAINING 域数；
持久 CLOSED OrderState 仍占一个 strict domain。Frozen payload/attempt 归属在 drain
后继续有效，另一个 origin 不能接管旧费用。

退休检查要求实际 primary/mirror 恰剩 descriptor、source 不早于 allocation、
root/aggregate 一致并覆盖最低费用，Floor 覆盖 descriptor 及 counter 各自最新 mutation；
严格后续 deletion source 加上完整 ledger/reference/writer/catalog/pin/retention authority
才允许 C4 原子删 descriptor 和更新 counters。当前 root 禁止独立退休，零 counter
仍保留 root 槽位。Helper 没有实际 allocation/delete backend 或生产 authority。

注册链路核对确认：membership/channel 可能先需要 incarnation ID。必须用独立前序
allocation source action 分配并返回结果后再注册，不能让带新 ID 的已签名注册 body
反过来派生自身 ID。该 request/result/control 协议及 Queue 轮换/legacy handover
source 配方明确保留为 B4/B6 未完成项，普通 QueueState successor 的不可变规则未放宽。

验证：

- `./gradlew spotlessApply test --tests '*TargetQuota*Test' checkstyleMain checkstyleTest checkTargetQuotaIncarnationVectors --console=plain`：8 类、154 项，无失败/错误/skip；新增 19 项。
- `./gradlew spotlessApply check --console=plain`：2020 项 Java tests，失败/错误 0、外部 skip 41；304 份 XML 全部归档。
- 1174 个源码/测试/向量/构建输入在最终完整检查前后 hash 一致。23 条独立向量，fixture SHA-256 `0ce61f14be4f1041c1fdf869e81a63fb44da5357869b059a3181720a315d4e48`。
- 最大样本保留两份 1 MiB Pulsar source 和 raw uint64 allocation/drain sequence，独立编码、摘要与固定费用一致；合法 CRC 的新 NV envelope 被旧 reader 拒绝。
- 74 个此前 quota/grant/payload/Message record 与活动 Lane 保护文件逐字不变；TargetKeyCodec 仅增加预留 tag。

`evidence/b4-incarnation-results.json` 绑定完整源码、文档、日志、保护文件、检查输入和
JUnit archive。B4 仍 IN_PROGRESS；完整 Source 控制、剩余 owner/reserve、原验收绑定
继续推进。C4 真实 Store/ledger/原子恢复与 D/E/F Broker、迁移、受保护旧路径清退未被
本批结构/计量测试替代。


### 2026-09-08：B4 首次 Target grant 的前序分配

基于 `b81a63c1a1fd37a89a08b9f2aa4600b16319e1ed`，单 agent 直接 main。
复用已经注册和签名的 grant Control 18/Apply kind 17；请求不包含待分配 ID，允许其
source 成为 membership/channel 注册前的首次 allocation 来源。NV 30 field 7 保存
完整 OPEN 历史来源，digest 移至 field 8；该 Draft 1 尚未启用 Target writer，不添加
旧七字段 decoder 回退。后续 grant 更新保留 origin，不能重定价或复活当前 descriptor。

View 显式包含冻结 lineage 并纳入 exact commit guard。首次非零 Target grant 要求
没有 prior origin/Target total，自己的 STATE/incarnation 可被 grant 容纳，再调用
mandatory capacity authority；它收到本次 candidate，零初始额度、SHARD 和普通更新
传 null。真正的 descriptor/counter/root absence、租户 cuts、物理/结果 reserve 与
原子写入仍由 C4 后端证明；候选 ID 不得在 durable Result 前发布到下游注册。

Grant activation root 槽位从单 source 扩为两 source + 完整 OPEN origin，短 Kafka
样本费用 3872 bytes；NV 33 自身仍独立计费及唯一 incarnation=1。源码复核补上了
next 为零但 prior 非零时不能省略 origin 的条件；修正前的完整检查已主动取消，最终
receipt 仅绑定修正后的完整成功检查。

验证：全部 163 项 quota tests 无失败/错误/skip，其中新增 9 项覆盖精确 candidate、
零初始后分配、down/up 保留、SHARD 不分配、已有零 total 拒绝、费用缺口、immutable
lineage/read guard、source metadata 派生差异和重算 digest 的错误 origin。独立向量
覆盖完整 signed grant 与两份最大 Pulsar source/raw uint64，Bookkeeping 上界同步。
完整 `./gradlew spotlessApply check --console=plain`：2029 项 Java tests，失败/错误 0、
外部 skip 41；304 份 XML 和最终 1174 个不变输入存入 source-bound receipt/archive。

证据为 `evidence/b4-grant-allocation-results.json` 及同名前缀 check/focused/final-docs
日志与 JUnit ZIP。B4 仍 IN_PROGRESS，未宣称完整 allocation source protocol、实际
Target Store writer/authority、Broker 恢复、root bootstrap、受控轮换或旧路径清退。


### 2026-09-08：B4 Target metadata 的实际归属与有限点读

基于 `6b461e50d67062126039a6541f50d737e7a742f9`，单 agent 直接 main。
新增 TargetQuotaMetadataRecords，复用现有 NV 12/13/17–20/22–25：physical identity、
dispatch/control、membership grant/policy 归 first allocation；queue/order/channel/
Native scope 按自身 incarnation，snapshot 按完整 referenced scope。完整 key/value、
physical/Source Shard/tenant/origin 均参与检查；membership source 须晚于 allocation。

每条实际记录只计自己的 STATE bytes，queue 与 strict state 提供已有唯一 cardinality；
retained/CLOSED/draining 不释放费用，依赖不重复计费。Record 保留最多四个 META 点读，
核对实际已有 before 记录及 owner/grant/physical/scope 的完整 type/payload。缺失、替换
和读失败拒绝，fatal Error 传播。Proposed insert 的 absence、完整触及键、权限/source
与 atomic ledger 仍需 C4 接入，四点计数不替代 A2/E6 字节/时间/物理 reserve。

验证：新增 12 项测试，全部 175 项 quota tests 无失败/错误/skip；覆盖五类共享归属、
所有依赖缺失/type/payload 改变、immutable copies、错误 Target/tenant/incarnation、
grant down-to-zero/drain 保留、membership source、queue/strict cardinality、channel/
Native 引用，以及 1 MiB physical identity 计费与 checked overflow。已有五组独立
compatibility/binding/membership/control/Native golden inputs 原样复用。

完整 `./gradlew spotlessApply check --console=plain` 通过：2041 项 Java tests，失败/
错误 0、外部 skip 41；305 份 XML 全部归档，1176 个最终输入在完整检查前后不变。
证据见 `evidence/b4-metadata-results.json` 与同名前缀日志、JUnit ZIP。B4 保持
IN_PROGRESS；Claim/Result/SystemMutation/evidence、共享 Shard owner、完整 reserve、
bootstrap/rotation/handover、原验收绑定以及实际 C4/D/E/F 工作继续实施。


### 2026-09-08：B4 本地 Claim 的双序号与有限计费计划

基于 `7c09c9759914f817fd07aaf3a81362fe3fcd4391`，单 agent 直接 main。
检查现有 Claim/revoke 的本地 batch 发现，它不推进 source sequence/SourcePosition。
本批为 TargetQuotaMutation 增加独立 localClaimOrdinal，避免本地 Claim 次数改变
恢复 replay 的 source 派生 incarnation。Source-only allocation/grant/bookkeeping/
payload owner/attempt budget 拒绝 local stamp；counter/total/aggregate revision
按各自实际 quota 写入递增。Source 入口继续严格推进；相同 source 下按 ordinal
排序，相同 ordinal 要求完整 stamp/digest 一致。Floor 不含 ordinal，需严格更晚
source 才能覆盖 local stamp，实际 lineage/catalog/pins/删除 authority 仍须证明。

prepareLocalClaim 最多读取四个既有 Target/tenant counter，严格 primary/mirror
incarnation 配对及逐维 delta，仅一个 Claim 的 7/8 与 STATE 可以改变。第二对可
承载冻结 Message owner 的 STATE 差额。强制 LocalClaimAuthority 校验 actual Claim/
原始 charge/完整 before-after/route/Owner/Store/source；旧 aggregate/counter bytes
在提交前重新核对，随后同 source 的 local commit 也会使旧计划失效。当前仅提供
有限 planner 和契约，未实现 Target Claim record、实际 authority 或 Store commit。

新增 15 项测试：三步独立 canonical golden、40 次 Claim/revoke、后续 source 派生
identity 历史无关、source-only 严格推进、authority/错误/fatal 传播、有限点读/配对/
维度和 cardinality 拒绝、跨 incarnation Message STATE、旧计划失效、grant/root
兼容、ordinal reset/unsigned 边界/耗尽、显式零字段拒绝与保守 Floor。两组独立
Python 向量通过；counter/aggregate/total 固定槽位各增加 11 bytes，样例 root
从 4896 增至 4929 bytes，其它 source-only 格式及已有 golden bytes 保持不变。

开发验证中先修正了误用 source sequence 的方向；编译检查发现并修正 getter 名称，
随后两处旧测试数据仍使用该序号，也已改为独立 ordinal。最终 focused/full 两次
验证均针对最终源代码，早期失败日志不作为通过证据。
完整 `./gradlew spotlessApply check --console=plain` 通过：2056 项 Java tests，失败/
错误 0，外部 skip 41；190 项 quota tests 全部无 skip。306 份 XML 归档，1179 个
输入在 full check 前后相同，证据见 `evidence/b4-local-claim-results.json` 与对应日志/
JUnit ZIP。B4 保持 IN_PROGRESS，原验收、完整 owners/reserves、C4/D/E/F 义务继续。


### 2026-09-08：B4 Claim 原始计费归属与消费条件

基于 `ab633d97afa2e31b7b073261bec95deb85cb701c`，单 agent 直接 main。
新增 TargetQuotaClaimCharge，预留 META 1b/NV 34：完整原始 Target work、Claim ID、
TARGET identity/tenant/accounting、Owner/Store、Claim sequence/deadline、冻结 execution
bytes、业务 Claim digest、local creation stamp 和 recovery lineage。62-byte key
含 Source Shard/Owner epoch/Claim ID。创建绑定 source-derived descriptor；draining
不自动取消已承诺工作，但实际业务与容量权威仍必须认证。

Projection 的 STATE 按实际 key/payload/NV/overhead 计费，execution 仅占 7/8，
不复制 payload/attempt/cardinality。实际业务 Claim 仍须自己的完整 precondition/
materialization 和独立 STATE；既有 Lane ClaimRecord NV 9 未改，不能充当 Target
格式。本地撤销校验同一完整 Owner/Store 与后续 local stamp；source consumption
要求严格后续 source-only stamp；两者强制 authority 验证原始 Claim/charge 和实际
消费。requireStored 对完整 key/type/payload 做 before guard。校验本身不删除或
释放；原子 Claim/Message/index/attempt/quota 更新与不确定写入恢复仍由 C4 接入。

新增 11 项测试覆盖 ordinary/Native/strict-order 三类独立 bytes、冻结费用和 key；
source-derived descriptor/draining（创建拒绝未来 descriptor 状态，历史投影可保留后续 drain）、
错误 attribution、Owner/Store/source/ordinal
消费条件、失败与 fatal Error 传播、完整 old bytes、immutable copies、unsigned
Claim sequence、Owner/encoding bounds、正确封装的 NV 34 被旧 reader 拒绝，以及
projection 贡献接入 local Claim/revoke delta 后原始费用恢复。开发验证修正了测试
中的 getter、容量向量维度和样式；最终 focused/full 均使用冻结后的最终输入。

完整 `./gradlew spotlessApply check --console=plain` 通过：2067 项 Java tests，失败/
错误 0，外部 skip 41；201 项 quota tests 全部无 skip。307 份 XML 归档，1183 个
输入在 full check 前后相同，见 `evidence/b4-claim-charge-results.json` 与日志/JUnit
ZIP。B4 原验收、完整业务 Claim/Result/其它 owners/reserves、交接配方及 C4/D/E/F
仍未完成，状态保持 IN_PROGRESS，未授予 active runtime/Broker/迁移权威。


### 2026-09-08：B4 不可变首次结果与独立物理审计

基于 `12afbd0e2afd92c6594c40dd3e29170f63af1b39`，单 agent 直接 main。
新增 TargetResultRecord schema 1，预留 DEDUPE 06–09 / NV 35。COMMAND/RESULT/SYSTEM
保存完整原结果与 source-only mutation、owner/tenant/accounting/lineage；RESULT
精确引用 COMMAND，两个 POSITION 分支分别引用 COMMAND 或 SYSTEM 并归 Shard。
后续物理重复增加自己的 POSITION 和 EVIDENCE 费用，首次逻辑结果 bytes/source/
owner 不变。COMMAND/POSITION 属 EVIDENCE，RESULT/SYSTEM 属 RESULT，不冒充 outbox。

成功 allocation 控制的 SYSTEM 结果可携带同 source 的完整 OPEN origin；拒绝其它
System 类型/错误状态、不同 source、draining 或 scope 不符 attachment。其 bytes
只计入本条 RESULT，不新增 descriptor cardinality。实际提交、受保护查询和后续
ID 使用仍须 C4 接入，构造计划不是已发布结果。

创建检查当前 descriptor/source 与完整 payload、强制实际 source/去重/业务 owner/
absence/read-set authority；查询副本与 POSITION 检查完整首记录引用，actual read
检查 key/type/full value。删除要求同 lineage Floor 覆盖原 source、严格后续 deletion
source 与实际 query/retry/replay/fence/pin/reference/删除权威；校验本身不释放费用。
外层 field/bytes、嵌套 protocol tuple 和 author branch/field 上限在解码前约束。

新增 15 项测试覆盖六组独立 bytes/key/charge、50 次物理位置推进保留首次结果、
query/reference/source/owner/lineage 错误、成功 allocation 返回值及拒绝分支、
mandatory authority/错误/fatal 传播、Floor/引用删除保护、immutable copies、类别
不重复计费、畸形/越界/legacy reader 拒绝。开发验证修正独立 CommandId 的 UUIDv7/
CRC 构造、冗余 cast 和样式；最终 focused/full 均针对最终冻结输入。

完整 `./gradlew spotlessApply check --console=plain` 通过：2082 项 Java tests，失败/
错误 0，外部 skip 41；201 项 quota 与 15 项结果测试全部无 skip。308 份 XML 归档，
1187 个输入在 full check 前后不变，见 `evidence/b4-result-record-results.json` 与
对应日志/JUnit ZIP。B4 保持 IN_PROGRESS，业务 Claim/其它共享记录/完整 reserve/
交接及原验收、C4 实际 source/query/dedupe/atomicity/recovery 和 D/E/F 均继续实施。


### 2026-09-08：B4 结果账本独立恢复核对与 decoded-owner 修正

基于 `afb6d3e2a686cc0e34bc7fc774e357985a1430f4`，单 agent 直接 main。
新增 TargetResultLedgerAudit，从完整 DEDUPE 06–09 记录和精确 META 1a owner 依赖
重建 primary/tenant contribution；总费用仅加 primary 一次，descriptor 自身费用
留给其独立账本。显式限制结果数、唯一 owner 数与实际 encoded bytes，任何预算/
解码/读取/引用/算术/权威失败均不返回部分通过结果。未使用 counter usage 作为输入。

每条记录检查完整 key/type/value、Shard/tenant/lineage/source frontier，owner
allocation/latest 与所有 result stamps 共用 source 历史一致性核对；同一物理 source
只能属于同一逻辑事件。遍历结束后解析完整集合中的 first COMMAND/SYSTEM 引用，
拒绝孤立或篡改引用，允许合法逆序扫描。显式 CompletenessAuthority 仍须由实际
同一 Store snapshot、Route/Owner/Store/source guard 实现；调用集合或小计不构成
完整 Store 证明，actual scanner 与全账本 counter/total/aggregate 恢复仍属 C4。

独立解码测试发现 TargetQuotaAccounting 缺少 equals/hashCode，原有 Claim 和
Target result 的 owner 校验错误依赖同一个 Java 对象。现按完整 canonical bytes
实现值相等、按稳定 digest 实现 hashCode，独立解码的相同 artifact 可合法匹配；
不同 artifact 仍拒绝。原六组 result bytes/key/charge 向量保持不变，新增四项独立
Python aggregate/encoded-byte 向量，未修改已有 wire 格式。

新增 11 项账本审计测试和 2 项 accounting/Claim 冷读回归测试：覆盖反序、预算边界、
错误/缺失 owner、同 source 冲突、缺失/变更 first、duplicate key、lineage/tenant/
metadata 错配、读取/完整性/fatal 失败和 defensive copies。开发首轮 26 项测试中
6 项因上述值比较缺陷失败；修正后最终 229 项 quota/result 专项均无 skip 通过。

完整 `./gradlew spotlessApply check --console=plain` 通过：2095 项 Java tests，失败/
错误 0，外部 skip 41；309 份 XML 归档，1189 个输入在 full check 前后不变，见
`evidence/b4-result-audit-results.json` 与对应日志/JUnit ZIP。结果账本小计不代表
全库独立恢复通过。B4 保持 IN_PROGRESS，其它 owners/reserves/交接及原验收、C4
实际 snapshot/source/query/dedupe/atomicity/recovery、A2/E6 资源证明和 D/E/F 继续。


### 2026-09-08：实现优先与 C4/C5 实际 Store 后端

基于 `2d3ceb99dbb4eb66881ac1cf7fb4a7253ae4488a`，按用户新执行顺序保持单 agent、
直接 main。立即增加实现/验证双状态和原 29 切片集中验证交接清单，不再逐批运行
全量回归或制作完整验收包。D/验证为主的 E7 与实际迁移清退留到最后集中阶段。

新增 ShardStore.openTarget 显式格式入口、TargetValueEnvelope 与 TargetStoreBackend，
实际连接已有 quota planners、精确 RocksDB read view、一次同步批量写以及完整结果
namespace/META 依赖读取。格式不符在身份重写前拒绝；Prepared 一次性使用；业务
键不允许覆盖后端派生账本和 source fixed metadata；local Claim 不推进 source。
完整 Worker/source authority、各业务 mutation、全部 ledger recovery 仍未完成。

本批仅运行编译/格式、两项实际 RocksDB 开发 smoke 和 Checkstyle：分别检查双向
格式拒绝且 identity 不变、source/aggregate 单次提交/重开/陈旧计划拒绝。初次 smoke
编译修正 Kafka source 的 String/UUID 实参类型，修正后两项通过。尚未验证业务 leaf/
mirror/total 混合写集故障、local Claim、实际结果遍历、完整恢复及运行装配；这些具体
项已进入集中交接，不将两项 smoke 推广为 C4/C5 VERIFIED。共享 Store 修改影响旧
runtime source，旧 receipt 仅作历史证据保留。


### 2026-09-08：C1/C4/E5 Message graph 与实际 head 提交

基于 `6a43211fe203a50258f13b9ca1d46f2be7b1488e`，继续单 agent、直接 main、实现优先。
新增 TargetMessageStore 与 TargetQueueHeadUpdater，扩展 Store Reader 的 bounded
exact-overlay first/projected 读取。完整 Message/Expiry/current work 差集、严格
serviceable head 和 queue heads 自动生成，计费 planner 接收最终写集后才提交。
所有 before 精确匹配；已有队头须符合持久最小项；新域拒绝孤立候选；缺失 sibling、
错误 Message/strict barrier/未决 attempts 不能通过被选中队头校验。

只执行编译/Checkstyle 与一项实际 RocksDB 开发 smoke：初始 Native Message 同批
建立 ordinary/native/Expiry 和 heads，终态同批删除索引、清空 heads、推进 source，
重复 Prepared 拒绝。复核后补充 overlay 原 before 与新域孤立候选检查并复跑该项。
复核另补全部变更 work 的 routing 与无 head strict state 的 queue 关联检查。
smoke 的 accounting/CommitGuard 使用显式测试 doubles，不证明真实计费、认证或
完整 Schedule/Claim/Admission；不制作完整验收包、不运行全量回归。源语义、完整
账本计费、strict 迟到/控制行为与 Worker 装配继续实现，集中验证逐项保留。
