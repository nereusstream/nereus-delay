# NDIP-1 目标模式执行提示词

## 1. 现在使用：H0 实施

下面的提示词可以直接复制到另一个 Codex 会话。当前 NDIP 状态是 Draft，因此目标故意只到
H0；完成 H0 后必须停止，不能自动进入完整语义改造。

```text
请进入目标模式并持续完成下面的目标：

目标：基于 Nereus Delay 最新远端 main，实现并验证 NDIP-1 的 H0 fail-closed。H0 必须同时
封闭 Managed early publish 和 AUTO_FAST 两条尚未闭环的真实 Pulsar 物理路径；不要实施
NDIP-1 H1-H6，不要添加 Pulsar .deliverAt(...)，不要修改当前权威 not-before 契约。

仓库：/Users/liusinan/apps/ideaproject/nereusstream/nereus-delay
设计基线：main@8915d21ed325a90ec305201ca85ab8daea3803dc
必读文档：
- /Users/liusinan/apps/ideaproject/nereusstream/nereus-delay/docs/ndip/NDIP-1/README.md
- /Users/liusinan/apps/ideaproject/nereusstream/nereus-delay/docs/ndip/NDIP-1/02-NDIP-1-Pulsar-Native-Delivery.md
- /Users/liusinan/apps/ideaproject/nereusstream/nereus-delay/docs/ndip/NDIP-1/03-实施计划.md
- /Users/liusinan/apps/ideaproject/nereusstream/nereus-delay/docs/ndip/NDIP-1/04-代码级目标设计.md

开始前：
1. 只读核对 git status、HEAD、origin/main 和 remote main；若 remote main 已不是上述基线，先
   对 H0 的五个生产入口和测试调用图做差异审计，再实施，不能机械套旧行号。
2. 当前共享工作树可能落后且含用户未跟踪的 docs/ndip/。不得清理、覆盖、暂存或提交这些
   文件。若需要写代码，使用从最新 remote main 创建的独立 worktree/branch；仍可从上述
   绝对路径读取设计文档。
3. 不修改、不整理任何与 H0 无关的 dirty/untracked 文件。不要 commit、push、开 PR 或清理
   运行资源，除非我在该会话另行明确授权。

H0 精确实现：
A. Managed outer gate
- 在 WorkerPhysicalPublishExecutor.submit(...) 中，identity 校验之后、现有 checkGate(...)
  之前执行 `actionAtEpochMs() < deliverAtEpochMs()` 检查；因此它也早于
  physicalAdapter.submit(...) 和任何 physical admission acquisition。
- 命中时生成 DEFINITELY_NOT_PUBLISHED / CAPABILITY_UNAVAILABLE，并走现有 source-log
  Outcome handoff；physical adapter/admission/delegate 调用数必须为 0。

B. Managed adapter gate
- 在 PinnedPulsarDestinationAdapter.publishOpen(...) 中，位于 timingPolicy.validate(...) 和
  transport request/transport call 之前做相同拒绝。
- direct adapter bypass Worker 时也必须 definitive no-publish，transport calls=0。

C. Managed real transport gate
- 在 PulsarClientArtifactDestinationTransport.publish(...) 中，位于 producer.newMessage()
  之前做相同拒绝。
- direct real transport call 必须 producer.newMessage=0、sendAsync=0。

D. AUTO_FAST adapter gate
- 在 PinnedPulsarNativeSubmissionAdapter.submitOpen(...) 中保留已有 resource/signature/time/
  credential/attempt/request 精确验证；对原本将调用 transport.send(request) 的有效请求，在该
  调用前返回 NativeDefinitelyNotQueued，code=CAPABILITY_UNAVAILABLE，proof kind=
  LOCAL_BEFORE_PRODUCER_OWNERSHIP。
- transport.send calls=0。不要把本地 H0 伪装为 PULSAR_GUARD_REJECTION。

E. AUTO_FAST real transport gate
- 在 PulsarClientArtifactSendTransport.send(PulsarNativeSendRequest) 中，位于 sendFrame(...) /
  producer.newMessage() 前返回 PulsarSendResult.DEFINITIVELY_NOT_PERSISTED，stable code=
  CAPABILITY_UNAVAILABLE。
- command-ingress 的 PulsarSendRequest overload 和其他 ordinary source/command transport 不得
  被误伤。

禁止修改：
- BoundedDestinationPublishAdapter
- PulsarDestinationTimingPolicy
- Destination/Capability Profile、wire、Store、Admission schema
- 任何 builder deliverAt/key/property/sequence 投影
- H1-H6 设计或权威文档结论

测试：
1. WorkerPhysicalPublishExecutorTest：early request 不取得 physical reservation、不调用
   adapter，且 exact definitive Outcome mutation 被排队。
2. DestinationAdapterTest：certified/early request 现在 definitive CAPABILITY_UNAVAILABLE，
   transport calls=0；ordinary Pulsar/Kafka 回归不变。
3. NativeSubmissionAdapterTest：有效 native request 返回 local definitive
   CAPABILITY_UNAVAILABLE，transport calls=0；现有 malformed/resource/signature/expiry 精确
   前置错误保持不变。
4. 新增 real-pulsar 无 Broker H0 smoke，使用可计数 Producer test double/proxy，分别 direct
   调用两个 real transport，断言 newMessage=0、sendAsync=0，并新增对应 Gradle task
   runRealPulsarH0Smoke。
5. 验证 close/race/exception 后 reservation/permit/buffer 无泄漏；ordinary Managed 仍可发送。

验证 gate：
- focused unit tests
- ./gradlew test
- ./gradlew check
- 使用明确的 source-locked P1 client classpath 运行 compileRealPulsar 和
  runRealPulsarH0Smoke；若当前环境没有该 classpath，不能伪造 PASS，报告 exact blocker 和
  已完成的其余验证。
- 检查 git diff、git status 和 checkProjectVersionMarkers；只报告本任务产生的 scoped diff。

工作方式：
- 先给出基线/工作树/H0 调用图的简短证据，再开始修改。
- 使用 apply_patch 编辑；保留他人改动。
- 每个风险判断以当前源码为准；文档与源码冲突时停止相关实现并报告，不自行扩大契约。
- 完成后给出：修改文件、每层 no-touch 证明、运行命令及结果、未运行 gate、剩余风险。
- H0 全部完成后停止；明确写“NDIP 仍为 Draft，H1-H6 未实施”。
```

## 2. 以后使用：Accepted 后完整实施

只有现行全局治理中的 Accepted receipt 已绑定 README 定义的 exact NDIP-1 normative package
digest，且
`DataResetAssessment` PASS 或一次性迁移设计也已被接受时，才使用下面的提示词。只修改
本目录的 `Status` 不算 Accepted；下面的提示词不是当前授权。

```text
请进入目标模式，按 docs/ndip/NDIP-1/ 的 Accepted 设计持续完成 NDIP-1 H1-H6。首先从
最新 remote main 建立独立 worktree，核对现行治理 Accepted receipt 与 exact NDIP package digest、
DataResetAssessment receipt、P1 source lock、工作树和所有前置 gate；任一不成立就停止并报告，
不能把 Draft 或 assessment 缺失当作实现授权。

以 04-代码级目标设计.md 为代码级契约，以 02 提案和 03 实施计划为治理/切片边界。严格按：
closed contracts/schema -> Store generation 与 dual READY -> signed policy authority ->
Scheduler/Claim/Admission -> production Journal/ownership marker -> P1 record/evidence ->
AUTO_FAST -> activation/reset -> fresh real-Pulsar evidence -> authority docs 的依赖顺序实施。

不得重新打开文档已经关闭的 R1-R6，不得增加 DispatchMode、reserved 第十属性、Subscription
限制、Visibility Guard、第二个独立 Scanner、永久双读或 AUTO_FAST 自动 resend。Admission
只绑定 RecordTemplate；Journal 后才产生 sequence 和 final PreparedRecord。preparedRecordHash
与 sendCommandSha256 是不同 domain，必须用 source-locked encoder 和 golden vectors证明投影，
不能要求两个 hash 数值相等。

每个切片都先重新审计当前调用图，做 scoped、可 review 的提交边界，运行 focused test、
./gradlew test、./gradlew check 和相应 source-locked integration gate。不要 commit、push、部署、
签发 ENABLED lease 或清理环境，除非我另行明确授权。signed DataResetManifest 只允许在环境
证据满足时生成；它通过前禁止 activation/cutover。持续报告实现、测试、真实 evidence、
activation readiness 和 NDIP status，不能把代码存在或文档完成写成 Implemented。
```
