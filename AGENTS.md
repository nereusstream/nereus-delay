# AGENTS.md

## 核心原则

交付当前任务需要的、可验证的最小合理实现。
减少无依据的复杂度，不降低已约定的正确性、安全性和兼容性。
完成度取决于目标是否实现，而不是代码量、测试数、文档数或抽象层数。

## 1. 控制任务范围

- 只做当前明确要求及其必要依赖。路线图、历史讨论和未来设计
  不是本次实现授权。用户只要求分析、评审或方案时，不修改代码。
- 先阅读相关实现、调用关系和已有测试，再做判断；
  按需要扩展调查，不默认扫描整个仓库或重新设计整个系统。
- 复杂任务开始前，用几句话说明目标、明确不做的事项和验证方式；
  简单任务直接处理，不默认创建计划文件或审批流程。
- 范围外问题可以简要报告，不顺手修复。
  普通、可逆的实现细节自行选择最简单的合理方案；
  涉及未明确的数据语义、公共接口或不可逆操作时，先澄清关键分歧。

## 2. 优先简单、直接的设计

- 优先复用现有实现和依赖。新增抽象应解决当前的具体问题，
  例如明确资源所有权、隔离实际依赖或消除实质性重复；
  “以后可能用到”不构成理由。
- 能用直接调用和少量明确步骤解决的问题，不包装成通用框架。
  不提前增加可插拔后端、策略层、配置开关或兼容路径。
- 新增线程、队列、异步调度、缓存或重试层，
  必须有当前需求或性能依据。
  已有组件的能力满足要求时，不再叠加同类机制。
- 允许为正确解决本次问题而做必要的局部重构；
  不顺便统一风格、批量改名、搬迁文件或清理无关代码。
  最小改动不等于用临时补丁掩盖根因。
- 不为便于 mock 而扭曲生产设计；
  可测试性改进应同时保持生产代码的职责和依赖关系清晰。

## 3. 保留正确性底线

- 不以简化为由削弱本次涉及的既有契约，
  例如持久化与成功确认、恢复、写者隔离、顺序、幂等和资源释放。
  这些是检查示例，不是要求每次补齐所有能力。
- 处理实际可达的 I/O 失败、超时不确定性、崩溃窗口和并发竞争；
  低概率的数据损坏风险不能因“少见”而忽略。
  不要吞错、伪造成功或静默改变语义。
- 不为缺乏依据的假设堆叠防御分支。
  依赖库或内部协议已有的保证，应先确认再使用；
  不擅自改变公共 API、持久化格式、默认行为或兼容承诺。

## 4. 按风险选择验证

- 先看已有覆盖。新增测试应对应变化的行为、
  明确的 bug 或独立的故障风险；
  已有测试足以覆盖时，不重复新增。
  不为凑覆盖率或测试数量扩展测试矩阵。
- 优先验证可观察行为和契约，不把内部实现细节固化为断言。
  除非调用顺序本身就是契约，否则不依赖大量 mock 交互断言。
- 选择能检验相关风险的最轻量方法：
  纯逻辑优先小型单测；
  真实存储、协议交互或恢复语义需要相应的集成验证。
  不能用 mock 成功代替真实语义验证，
  也不为小改动新建大型测试框架。
- 迭代时先运行最小相关测试集；
  共享契约、公共组件或跨模块行为受影响时，再扩大范围。
  保留仓库规定的必要检查和 CI，不默认每次运行全量测试。
- 无代码、环境变化且无新验证目的时，不反复执行同一检查。
  并发、偶发故障和性能统计需要重复时，应明确所验证的问题；
  环境阻塞做有限定位，避免无目的重跑。
- 不为获得通过结果而跳过有效测试、弱化断言或硬编码测试输入。
  契约确已变更时，可更新或删除过时测试，
  但应说明依据并保留仍有效的覆盖。
- 性能结论需要可复现、语义等价且条件可比的前后对照；
  不能靠降低持久化、安全性或一致性要求制造提升。
  非性能任务不默认展开完整基准矩阵，
  未经测量的收益只作为假设。

## 5. 评审聚焦实际问题，完成后停止

- 评审意见应说明触发条件、相关代码路径和实际影响，
  区分正确性问题、需求缺口与可选建议。
  不把风格偏好或假设性的未来需求当作阻塞项；
  没有实质问题时可以明确说明。
- 完成本次验收、执行适用检查且没有已知的相关阻塞问题后停止。
  不自动进入下一阶段，不反复开展“再找一些问题”的评审，
  不因可选建议继续扩大实现。
- 发现新的证据或用户要求进一步验证时再展开。
  验收未满足或关键验证受阻时，如实报告未完成事项，
  不把“部分通过”描述成“全部完成”。

## 6. 简洁交付，避免附带产物

- 最终说明实际改动、执行的验证及结果、尚未验证的风险。
  没有执行的命令不得写成通过；
  局部验证不得扩展成对整个系统正确性、可靠性或性能的保证。
- 根据本次变更同步确有必要的既有文档。
  除非任务要求，不额外生成设计书、验收台账、
  测试总结文件或新的管理流程；必要说明放在回复中。
- 清理由本次任务创建且不再需要的临时文件，
  不删除用户原有文件或改动，
  不擅自调整无关依赖、构建设置或本文件来绕过约束。
